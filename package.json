{
  "name": "jy-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r {index.html,css,js,assets} dist/",
    "generate-placeholders": "node scripts/generate-placeholders.js",
    "setup": "npm run generate-placeholders",
    "test": "node test/main.js"
  },
  "dependencies": {
    "canvas": "^2.11.2"
  },
  "devDependencies": {
    "express": "^4.18.2"
  }
}
